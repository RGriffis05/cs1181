/**
 * The Driver class serves as the entry point for the Tic-Tac-Toe application.
 * It launches the main menu interface where the user can choose to start the game
 * or view the instructions.
 */
public class Driver {

    /**
     * The main method that starts the application by displaying the MainMenu.
     *
     * @param args command-line arguments (not used)
     */
    public static void main(String[] args) {
        new MainMenu();
    }
}